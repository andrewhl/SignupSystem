<% provide(:title, 'Sign up') %>
<h2>Sign up</h2>

<%= form_for(@user) do |f| %>
<table>
	<%= render 'fields', f: f %>
	  <div class="actions">
	    <%= f.submit "Sign up" %>
	  </div>
	<% end %>
</table>